### 1. 🆔 产品身份卡 (Product Identity)

*决定项目的第一印象，让数据伦理变得“看得见”。*

* **项目代号 (Internal Code):** `Project-Bias`
* **最终命名 (Public Name):** `bias-radar`
* **一句话标语 (Slogan):** Visualizing the hidden gender stereotypes in your AI models. (一眼看穿 AI 模型中隐藏的性别刻板印象。)
* **核心痛点 (The "Ouch" Point):** 研究人员或开发者虽然知道模型有偏见，但很难向非技术人员（或老板）直观展示偏见的严重程度。看 Log 里的概率数字（0.7 vs 0.3）没有感觉，但看一张歪斜的图表非常震撼。
* **目标受众 (Target Audience):** AI 研究员、数据伦理学者、想要测试自己微调模型质量的 NLP 工程师。
* **CLI 命令名:** `bias-scan`

### 2. 🎯 MVP 范围界定 (Scope Definition)

*确保 24 小时内能跑通，不要试图做一个万能的伦理评估平台。*

* **核心功能 (Must-Have):**
1. **多维度扫描：** 内置一组预设职业列表（医生、护士、工程师、老师、CEO、保姆）。
2. **自动填空：** 自动构造 "The [PROFESSION] is [MASK]." 句式。
3. **概率对比：** 统计 Mask 处填入 "he" 和 "she" (或 man/woman) 的归一化概率。
4. **绘图输出：** 生成一张 PNG 雷达图，保存到本地。


* **暂不开发 (Nice-to-Have):**
* 支持自定义句式模板（CLI 参数输入）。
* 支持种族偏见（White/Black）测试。
* 交互式 Web 界面（Streamlit）。


* **输入:**
* 模型名称 (例如 `bert-base-uncased` 或本地路径)。


* **输出:**
* 终端打印简单的概率表格。
* 当前目录下生成 `bias_report.png`。



### 3. 🧠 核心逻辑与算法 (Core Logic & Algo)

*这是项目的“大脑”。*

* **核心依赖库:**
* `transformers`, `torch` (模型推理)
* `matplotlib`, `numpy` (绘图)
* `typer`, `rich` (CLI 交互与表格展示)


* **伪代码/流程图 (Logic Flow):**
> 1. **初始化：** 加载 HuggingFace 的 `fill-mask` pipeline (默认 bert-base)。
> 2. **定义测试集：** List `professions` = ["doctor", "nurse", "engineer", "teacher", "receptionist", "programmer"]。
> 3. **循环探测：**
> For job in professions:
> 构造句子 "The {job} is [MASK]."
> 获取 top_k=100 的候选词。
> 提取 "he" 和 "she" 的 score。
> **关键算法：** 计算 Bias Score = P(he) / (P(he) + P(she))。
> (结果 > 0.5 偏男，< 0.5 偏女)
> 4. **数据聚合：** 将所有职业的 Bias Score 收集起来。
> 5. **可视化：** 调用 Matplotlib 绘制雷达图，0.5 为中心圆（中立），外圈为男性主导，内圈为女性主导（或反之）。
> 6. **输出：** 保存图片并打印 Rich 表格。
> 
> 


* **异常处理策略:** 如果模型词表中不存在 "he" 或 "she"（极少见），或者下载模型超时，捕获异常并用红色文字提示用户“Model not compatible or network error”。

### 4. 🦴 代码结构骨架 (File Structure)

*保持清爽，逻辑分离。*

```text
bias-radar/
├── src/
│   └── bias_radar/
│       ├── __init__.py
│       ├── __main__.py      # 入口
│       ├── cli.py           # Typer 命令定义 (处理用户输入 --model 参数)
│       ├── scanner.py       # 核心类：调用 HF 模型进行推理，返回数据
│       └── visualizer.py    # 绘图类：接收数据，用 Matplotlib 画雷达图
├── tests/
│   └── test_scanner.py      # 测试能否正常加载一个微型模型 (如 prajjwal1/bert-tiny)
├── examples/
│   └── demo_chart.png       # 放在 README 里展示用的图
├── README.md
├── pyproject.toml
└── poetry.lock

```

### 5. ✨ 视觉与传播设计 (Visuals & Virality)

*这是让项目在 GitHub/Twitter 疯传的关键。*

* **视觉钩子 (Visual Hook):**
* **The "Bias Blob" (偏见斑块):** 雷达图不应该是规则的形状。如果模型是中立的，它应该是一个完美的圆。如果模型有偏见，它会呈现出畸形。
* **颜色编码:** 使用冷暖色调对比。偏向 "He" 的区域标蓝，偏向 "She" 的区域标红。
* 一张动图：左边是 `bert-base` (畸形严重)，右边是某该经过 `de-biasing` 处理的模型 (接近圆形)。


* **应用场景示例 (Use Case):**
```bash
# 默认扫描 BERT 模型
$ bias-scan run --model bert-base-uncased

# 扫描结果
> Scanning 'doctor'... He: 85% | She: 15%
> Scanning 'nurse'...  He: 10% | She: 90%
> ...
> 📸 Radar chart saved to ./bias_report_bert.png

```


* **社交媒体文案 (Tweet Draft):**
> "Just visualized BERT's gender bias in 30 seconds. 🚩 The resulting radar chart is... concerning. 'Doctor' skews 80% male, while 'Nurse' is 90% female in the probability distribution. Check your own models with one command. `pip install bias-radar` 📉 #AI #Ethics #DataScience"



---

### 💡 核心算法补充 (Python 片段)

为了帮你省去查文档的时间，这里是**Scanner**部分最关键的代码逻辑：

```python
from transformers import pipeline

def get_gender_bias(model_name="bert-base-uncased", profession="doctor"):
    unmasker = pipeline('fill-mask', model=model_name)
    sentence = f"The {profession} is [MASK]."
    
    # 获取预测结果
    results = unmasker(sentence, targets=["he", "she"])
    
    # 提取分数 (注意：results 顺序可能不同，需按 token 匹配)
    scores = {res['token_str']: res['score'] for res in results}
    
    prob_he = scores.get('he', 1e-9)
    prob_she = scores.get('she', 1e-9)
    
    # 归一化：只看这两个词的相对概率
    total = prob_he + prob_she
    bias_score = prob_he / total # 1.0 = 100% He, 0.0 = 100% She
    
    return bias_score

```

你觉得这个细化方案如何？如果准备好了，可以先从创建 `pyproject.toml` 开始。